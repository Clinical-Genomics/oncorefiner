nextflow_workflow {

    name "Test Subworkflow PREPARE_REFERENCES"
    script "../main.nf"
    workflow "PREPARE_REFERENCES"
    tag "subworkflows"
    tag "prepare_references"
    config "./nextflow.config"

    test("Test preparing VEP cache: no untarring needed") {

        when {
            params {
                outdir = "$outputDir"
            }
            workflow {
                """
                input[0] = params.pipelines_testdata_base_path + 'reference/GRCh38/vep_cache/'
                """
            }
        }

        then {
            // Check untarred directory and files
            def output_directories_files = getAllFilesFromDir(params.outdir, relative: true, includeDir: true)

            assertAll(
                { assert workflow.success },
                { assert snapshot(
                    // Check the name of the output directory
                    workflow.out.vep_resources[0][0].tokenize('/').last(),
                    // Check that no ouput files or directories are produced
                    output_directories_files
                ).match() }
            )
        }
    }

    test("Test preparing VEP cache: untarring needed") {

        when {
            params {
                outdir = "$outputDir"
            }
            workflow {
                """
                input[0] = params.pipelines_testdata_base_path + 'reference/GRCh38/vep_cache.tar.gz'
                """
            }
        }

        then {
            // Check untarred directory and files
            def output_directories_files = getAllFilesFromDir(params.outdir, relative: true, includeDir: true)

            assertAll(
                { assert workflow.success },
                { assert snapshot(
					// Check the name of the output directory
                    workflow.out.vep_resources[0][0].tokenize('/').last(),
                    // Check untarred directory and files
                    output_directories_files
                ).match() }
            )
        }
    }

}
