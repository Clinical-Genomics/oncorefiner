nextflow_pipeline {

    name "Test pipeline ONCOREFINER without providing optional inputs - stub"
    script "../main.nf"
    tag "pipeline"
    tag "conditional_inputs_stub"

    test("No `genmod_score_config_*` with '-profile test' - stub") {

        options "-stub"

        when {
            params {
                outdir = "$outputDir"
                genmod_score_config_snv = null
                genmod_score_config_sv  = null
            }
        }

        then {
            // stable_name: All files + folders in ${params.outdir}/ with a stable name
            def stable_name = getAllFilesFromDir(params.outdir, relative: true, includeDir: true, ignore: ['pipeline_info/*.{html,json,txt}'])
            // pipeline versions.yml file for multiqc from which Nextflow version is removed because we test pipelines on multiple Nextflow versions
            def versions = removeNextflowVersion("$outputDir/pipeline_info/oncorefiner_software_mqc_versions.yml")
            assertAll(
                { assert workflow.success},
                // Assert that "GENMODE_SCORE" is not run
                { assert "GENMODE_SCORE" !in versions.keySet()},
                { assert snapshot(
                    // List of software versions run in the pipeline
                    versions,
                    // All stable file names, with a relative path
                    stable_name,
                ).match() }
            )
        }
    }

    test("No `vep_plugin_files` with '-profile test' - stub") {

        options "-stub"

        when {
            params {
                outdir = "$outputDir"
                vep_plugin_files = null
            }
        }

        then {
            // stable_name: All files + folders in ${params.outdir}/ with a stable name
            def stable_name = getAllFilesFromDir(params.outdir, relative: true, includeDir: true, ignore: ['pipeline_info/*.{html,json,txt}'])
            // pipeline versions.yml file for multiqc from which Nextflow version is removed because we test pipelines on multiple Nextflow versions
            def versions = removeNextflowVersion("$outputDir/pipeline_info/oncorefiner_software_mqc_versions.yml")
            assertAll(
                { assert workflow.success},
                // Assert that "ENSEMBLVEP_VEP" runs when `vep_plugin_files` is not provided
                { assert "ENSEMBLVEP_VEP" in versions.keySet()},
                { assert snapshot(
                    // List of software versions run in the pipeline
                    versions,
                    // All stable file names, with a relative path
                    stable_name,
                ).match() }
            )
        }
    }
    test("No `cnv_gene_tsv` and `cnv_segment_tsv` with '-profile test' - stub") {

        options "-stub"

        when {
            params {
                outdir = "$outputDir"
                cnv_segment_tsv = null
                cnv_gene_tsv = null
            }
        }

        then {
            // stable_name: All files + folders in ${params.outdir}/ with a stable name
            def stable_name = getAllFilesFromDir(params.outdir, relative: true, includeDir: true, ignore: ['pipeline_info/*.{html,json,txt}'])
            // pipeline versions.yml file for multiqc from which Nextflow version is removed because we test pipelines on multiple Nextflow versions
            def versions = removeNextflowVersion("$outputDir/pipeline_info/oncorefiner_software_mqc_versions.yml")
            assertAll(
                { assert workflow.success},
                // Assert that "RMARKDOWNNOTEBOOK" is not run
                { assert "RMARKDOWNNOTEBOOK" !in versions.keySet()},
                { assert snapshot(
                    // List of software versions run in the pipeline
                    versions,
                    // All stable file names, with a relative path
                    stable_name,
                ).match() }
            )
        }
    }

    test("No GENS support with '-profile test' - stub") {

        options "-stub"

        when {
            params {
                outdir = "$outputDir"
                amber_baf_tsv_gz = null
                cobalt_ratio_pcf_normal = null
                cobalt_ratio_pcf_tumor = null
            }
        }

        then {
            // stable_name: All files + folders in ${params.outdir}/ with a stable name
            def stable_name = getAllFilesFromDir(params.outdir, relative: true, includeDir: true, ignore: ['pipeline_info/*.{html,json,txt}'])
            // pipeline versions.yml file for multiqc from which Nextflow version is removed because we test pipelines on multiple Nextflow versions
            def versions = removeNextflowVersion("$outputDir/pipeline_info/oncorefiner_software_mqc_versions.yml")
            assertAll(
                { assert workflow.success},
                // Assert that "PREPARE_AMBER_FOR_GENS" and "PREPARE_COBALT_FOR_GENS" are not run
                { assert "PREPARE_AMBER_FOR_GENS" && "PREPARE_COBALT_FOR_GENS" !in versions.keySet()},
                { assert snapshot(
                    // List of software versions run in the pipeline
                    versions,
                    // All stable file names, with a relative path
                    stable_name,
                ).match() }
            )
        }
    }

}
